import { ImgPreloaderDirective } from './img-preloader.directive';

describe('ImgPreloaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgPreloaderDirective();
    expect(directive).toBeTruthy();
  });
});
